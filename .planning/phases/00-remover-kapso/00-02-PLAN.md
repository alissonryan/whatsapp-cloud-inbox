---
phase: 00-remover-kapso
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/app/api/media/[mediaId]/route.ts
  - src/app/api/messages/interactive/route.ts
  - src/app/api/messages/send/route.ts
  - src/app/api/templates/route.ts
  - src/app/api/templates/send/route.ts
  - src/lib/meta/template-payload.ts
autonomous: true
must_haves:
  truths:
    - "A app consegue enviar mensagens (texto/midia/template/interativo) direto via Meta Graph API, sem Kapso."
    - "A app consegue listar templates direto via Meta Graph API."
    - "O browser consegue baixar midia via proxy server-side (sem expor token no client)."
  artifacts:
    - "Rotas `/api/messages/*`, `/api/templates*`, `/api/media/*` usando `src/lib/meta/*`"
    - "Helper para montar payload de template sem `buildTemplateSendPayload` do Kapso"
  key_links:
    - "Upload de midia -> id retornado -> send message referenciando id"
    - "Resolve media URL (GET /{MEDIA_ID}) -> download (GET {MEDIA_URL} com Bearer)"
    - "Payloads (template/interactive) compativeis com Cloud API"
---

<objective>
Migrar as rotas de envio/listagem de templates/midia para usar o client Meta implementado na Fase 0 / Plano 1, removendo chamadas Kapso.

Purpose: manter as rotas `/api/*` existentes, mas com implementacao propria e oficial.
Output: rotas server-side chamando a Cloud API direto (Graph API).
</objective>

<execution_context>
@.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/phases/00-remover-kapso/00-CONTEXT.md
@.planning/phases/00-remover-kapso/00-RESEARCH.md

# Rotas atuais (Kapso):
@src/app/api/messages/send/route.ts
@src/app/api/messages/interactive/route.ts
@src/app/api/templates/route.ts
@src/app/api/templates/send/route.ts
@src/app/api/media/[mediaId]/route.ts

# Meta client:
@src/lib/env.ts
@src/lib/meta/whatsapp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar envio de texto/midia e proxy de midia para Meta</name>
  <files>
src/app/api/messages/send/route.ts
src/app/api/media/[mediaId]/route.ts
  </files>
  <action>
Atualizar as rotas para usar `src/lib/meta/whatsapp.ts` + `src/lib/env.ts` (sem `whatsappClient` do Kapso):

- `POST /api/messages/send`:
  - Mantem contrato atual: multipart/form-data com `to`, `body` opcional, `file` opcional.
  - Se houver `file`: fazer `uploadMedia` primeiro; depois enviar a mensagem com o `id` retornado (tipo correto).
  - Se nao houver `file`: enviar texto.
  - Retornar JSON com status e ids relevantes (ex: message id quando disponivel).
  - Padronizar erros: 400 para input invalido; 500 para falhas Graph API (incluir message sanitizada).

- `GET /api/media/:mediaId`:
  - Chamar `getMedia` (GET `/{MEDIA_ID}`) para obter `url` e `mime_type`.
  - Baixar `url` com Bearer token e responder como stream/bytes com `Content-Type` correto.
  - Cache-control curto/razoavel (URL expira rapido, mas o proxy pode cachear bytes por um tempo).
  </action>
  <verify>
`npm run build`
  </verify>
  <done>
As duas rotas nao importam mais Kapso e usam somente o client Meta.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrar envio de mensagem interativa (buttons) para Meta</name>
  <files>
src/app/api/messages/interactive/route.ts
  </files>
  <action>
Substituir `whatsappClient.messages.sendInteractiveButtons` por chamada Cloud API via `sendInteractiveButtons`:

- Manter validacoes:
  - `buttons.length` 1..3
  - `title` max 20 chars
- Montar payload `type: "interactive"` com `interactive.type: "button"`, `body.text` e `action.buttons`.
- Header opcional de texto quando fornecido.
  </action>
  <verify>
`npm run build`
  </verify>
  <done>
Rota envia interactive buttons sem depender do SDK Kapso.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrar templates (listar + enviar) e remover buildTemplateSendPayload (Kapso)</name>
  <files>
src/app/api/templates/route.ts
src/app/api/templates/send/route.ts
src/lib/meta/template-payload.ts
  </files>
  <action>
Atualizar templates para Graph API direto:

- `GET /api/templates`:
  - Usar `listTemplates({ wabaId, limit })`.
  - Manter shape `{ data, paging }` usada pelo frontend.

- `POST /api/templates/send`:
  - Remover `buildTemplateSendPayload` do Kapso.
  - Criar helper `src/lib/meta/template-payload.ts` que monta `template` payload compativel com Cloud API:
    - `name`, `language.code`
    - `components`: HEADER/BODY/BUTTON (url button) com `parameters`.
  - Reaproveitar a logica atual de `parameterInfo` (TemplateParameterInfo) para mapear valores para components corretos.
  - Enviar via `sendTemplate`.
  </action>
  <verify>
`npm run build`
  </verify>
  <done>
Listagem e envio de template funcionam sem imports do Kapso.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passa.
- `rg -n \"@kapso/whatsapp-cloud-api\" src/app/api/messages src/app/api/templates src/app/api/media` nao retorna resultados.
</verification>

<success_criteria>
- Fluxos de envio (text/media/template/interactive) sao feitos via Meta Graph API.
- Midia funciona via proxy server-side.
- Nenhuma das rotas deste plano depende de Kapso.
</success_criteria>

<output>
After completion, create `.planning/phases/00-remover-kapso/00-02-SUMMARY.md`.
</output>

