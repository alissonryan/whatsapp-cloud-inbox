---
phase: 00-remover-kapso
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - src/lib/meta/graph.ts
  - src/lib/meta/whatsapp.ts
autonomous: true
user_setup:
  - service: meta
    why: "Chamar a WhatsApp Cloud API direto (Graph API)"
    env_vars:
      - name: META_ACCESS_TOKEN
        source: "Meta Business Manager (System User token) / ou token de acesso valido para WABA"
      - name: WHATSAPP_PHONE_NUMBER_ID
        source: "WhatsApp Manager -> Phone numbers"
      - name: WHATSAPP_WABA_ID
        source: "WhatsApp Manager -> Account tools"
must_haves:
  truths:
    - "O backend consegue montar requests para a Graph API com Bearer token e tratar erros de forma consistente."
    - "Existe uma camada unica (lib) para chamadas WhatsApp Cloud API (send, templates, media) sem dependencias Kapso."
  artifacts:
    - "src/lib/env.ts (validacao/leitura de env vars da Meta)"
    - "src/lib/meta/graph.ts (fetch wrapper: JSON + erro padronizado)"
    - "src/lib/meta/whatsapp.ts (operacoes WhatsApp Cloud API usadas pelo app)"
  key_links:
    - "Authorization: Bearer <META_ACCESS_TOKEN> aplicado em todas as chamadas"
    - "Base URL e Graph API version centralizadas para evitar divergencia entre endpoints"
---

<objective>
Criar um client proprio para a Meta (Graph API / WhatsApp Cloud API) e uma camada de env/config padronizada, para substituir o uso do SDK Kapso nas rotas `/api/*`.

Purpose: desbloquear a remocao completa do Kapso sem reescrever a app inteira de uma vez.
Output: modulos reutilizaveis para chamadas da Cloud API (send/media/templates) + validacao de env.
</objective>

<execution_context>
@.agents/skills/gsd/workflows/execute-plan/SKILL.md
@.agents/skills/gsd/references/verification-patterns/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-remover-kapso/00-CONTEXT.md
@.planning/phases/00-remover-kapso/00-RESEARCH.md

# Baseline atual (Kapso) para referencia de funcoes e payloads:
@src/lib/whatsapp-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar camada de env/config para Meta WhatsApp</name>
  <files>
src/lib/env.ts
  </files>
  <action>
Criar um helper pequeno e deterministico (ex: `getMetaWhatsappEnv()`), que:

- Leia e valide `META_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_WABA_ID`.
- Exponha tambem `META_GRAPH_VERSION` com default (ex: `v24.0`) e `META_GRAPH_BASE_URL` com default `https://graph.facebook.com`.
- Gere mensagens de erro claras (ex: "META_ACCESS_TOKEN is required") para facilitar setup.
- Nao inclua log de segredos.
  </action>
  <verify>
`npm run build`
  </verify>
  <done>
Imports TypeScript funcionam e o build passa; env helper falha com erro legivel quando variaveis obrigatorias estao ausentes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar fetch wrapper da Graph API (JSON + errors)</name>
  <files>
src/lib/meta/graph.ts
  </files>
  <action>
Criar um wrapper para `fetch` (ex: `graphFetchJson`) que:

- Receba `path` (ex: `/{PHONE_NUMBER_ID}/messages`) e query params opcionais.
- Injete `Authorization: Bearer ...` e `Content-Type` quando apropriado.
- Faça parse de erros da Graph API (status != 2xx) e lance um erro com: status, code, message, details (quando existir).
- Suporte retornar `Response` (ou `ArrayBuffer`) para casos de download de midia sem parse JSON.
  </action>
  <verify>
`npm run build`
  </verify>
  <done>
Existe uma funcao unica para chamadas Graph; erros nao ficam como "Failed to fetch" generico.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implementar operacoes WhatsApp Cloud API usadas pelo app</name>
  <files>
src/lib/meta/whatsapp.ts
  </files>
  <action>
Criar funcoes que encapsulem os endpoints do WhatsApp Cloud API que o app usa hoje:

- Send:
  - sendText
  - uploadMedia
  - sendImage / sendVideo / sendAudio / sendDocument (pode ser um helper generico + wrappers)
  - sendTemplate
  - sendInteractiveButtons (interactive type=button)
- Templates:
  - listTemplates (edge `/{WABA_ID}/message_templates`)
- Media:
  - getMedia (GET `/{MEDIA_ID}` -> url + mime_type)
  - downloadMedia (GET {MEDIA_URL} com Bearer)

Regras:

- Sempre incluir `messaging_product: "whatsapp"`.
- Manter tipagem minima (inputs/outputs) para reduzir regressao.
- Nao incluir nada relacionado a Kapso.
  </action>
  <verify>
`npm run build`
  </verify>
  <done>
Rotas `/api/*` conseguem migrar para usar essas funcoes sem depender do SDK Kapso.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passa.
- Nao existe import de `@kapso/whatsapp-cloud-api` nos novos modulos `src/lib/meta/*`.
</verification>

<success_criteria>
- Helpers de env e Graph API criados e prontos para serem usados pelas rotas.
- Nenhuma dependencia nova do Kapso foi adicionada.
</success_criteria>

<output>
After completion, create `.planning/phases/00-remover-kapso/00-01-SUMMARY.md`.
</output>

