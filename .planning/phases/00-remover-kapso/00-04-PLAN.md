---
phase: 00-remover-kapso
plan: 4
type: execute
wave: 3
depends_on: [2, 3]
files_modified:
  - README.md
  - package.json
  - package-lock.json
  - src/lib/whatsapp-client.ts
  - .env.example
autonomous: false
must_haves:
  truths:
    - "O repo nao depende mais de Kapso (sem SDK, sem env vars Kapso, sem URLs Kapso)."
    - "A documentacao ensina setup direto na Meta (token + ids) e explica a necessidade de webhooks/DB para inbox."
  artifacts:
    - "README.md sem referencias Kapso"
    - ".env.example com env vars Meta"
    - "package.json sem `@kapso/whatsapp-cloud-api`"
  key_links:
    - "Busca global (`rg kapso`) deve retornar vazio (ou apenas historico em planning)"
    - "Build continua passando apos remover dependencia"
user_setup:
  - service: node/npm
    why: "Atualizar lockfile sem dependencia Kapso"
    env_vars: []
---

<objective>
Remover completamente Kapso do codebase (dependencias, docs e branding), deixando o projeto pronto para evoluir a integracao oficial com a Meta.

Purpose: fechar a Fase 0 com uma base limpa e alinhada com a integracao direta.
Output: repo sem Kapso + docs/env atualizados.
</objective>

<execution_context>
@.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/meta-whatsapp.md
@README.md
@package.json
@src/lib/whatsapp-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover dependencia Kapso e codigo morto</name>
  <files>
package.json
package-lock.json
src/lib/whatsapp-client.ts
  </files>
  <action>
- Remover `@kapso/whatsapp-cloud-api` do `package.json`.
- Atualizar `package-lock.json` para refletir a remocao (via `npm install` ou ajuste equivalente).
- Apagar ou refatorar `src/lib/whatsapp-client.ts` (Kapso) para:
  - ou ser removido se nao estiver mais em uso
  - ou virar um wrapper local (sem Kapso) apontando para `src/lib/meta/*`
- Garantir que nao exista mais import de `@kapso/whatsapp-cloud-api` no `src/`.
  </action>
  <verify>
`rg -n \"@kapso/whatsapp-cloud-api\" src -S` e `npm run build`
  </verify>
  <done>
Build passa e o repositorio nao tem dependencia do SDK Kapso.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar README e criar .env.example para setup direto na Meta</name>
  <files>
README.md
.env.example
  </files>
  <action>
- Atualizar `README.md` para remover:
  - links `app.kapso.ai`, `api.kapso.ai`, `gokapso/*`
  - mencoes a `KAPSO_API_KEY` e `WHATSAPP_API_URL`
- Documentar env vars novas (exemplos):
  - `META_ACCESS_TOKEN`
  - `WHATSAPP_PHONE_NUMBER_ID`
  - `WHATSAPP_WABA_ID`
  - `META_APP_SECRET` (necessario para webhooks na Fase 1)
  - `META_WEBHOOK_VERIFY_TOKEN` (Fase 1)
  - `NEXT_PUBLIC_INBOX_DEV_ALLOW_FREEFORM` (dev override do 24h window)
- Criar `.env.example` com placeholders.
- Explicar claramente: inbox/historico depende de webhooks + DB (Fase 1).
  </action>
  <verify>
`rg -n \"kapso\\.ai|gokapso|KAPSO_API_KEY|WHATSAPP_API_URL\" -S .`
  </verify>
  <done>
README nao menciona Kapso e existe `.env.example` alinhado com Meta direta.
  </done>
</task>

</tasks>

<verification>
- `rg -n \"kapso\" -S src README.md package.json` nao retorna resultados relevantes.
- `npm run build` passa.
</verification>

<success_criteria>
- Repo limpo de Kapso (codigo, docs, dependencias).
- Setup direto Meta documentado.
</success_criteria>

<output>
After completion, create `.planning/phases/00-remover-kapso/00-04-SUMMARY.md`.
</output>

