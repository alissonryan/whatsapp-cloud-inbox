---
phase: 00-remover-kapso
plan: 3
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/api/conversations/route.ts
  - src/app/api/messages/[conversationId]/route.ts
  - src/components/conversation-list.tsx
  - src/components/message-view.tsx
autonomous: true
must_haves:
  truths:
    - "A UI nao quebra quando ainda nao existe DB/webhook; ela entra em um modo 'send-only' explicito."
    - "E possivel iniciar um chat informando um numero e enviar mensagens mesmo sem historico."
    - "As rotas de inbox (conversations/messages) nao fazem mais chamadas Kapso."
  artifacts:
    - "Rotas `/api/conversations` e `/api/messages/:conversationId` sem Kapso (stub send-only)."
    - "UI com 'start new chat' por numero e feedback claro sobre historico (Fase 1)."
  key_links:
    - "ConversationList consegue selecionar um chat mesmo sem retorno do backend."
    - "MessageView suporta estado local (optimistic) quando backend nao tem historico."
---

<objective>
Remover o acoplamento do Kapso nas rotas de inbox (conversations/messages) e deixar o frontend utilizavel em modo 'send-only' ate a Fase 1 (DB + webhooks).

Purpose: permitir validar integracao de envio com a Meta sem perder a UX basica da UI.
Output: endpoints stub + UI com start new chat e mensagens outbound locais.
</objective>

<execution_context>
@.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/phases/00-remover-kapso/00-CONTEXT.md
@.planning/ROADMAP.md

# UI atual:
@src/components/conversation-list.tsx
@src/components/message-view.tsx

# Rotas atuais (Kapso):
@src/app/api/conversations/route.ts
@src/app/api/messages/[conversationId]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever endpoints de inbox para modo send-only (sem Kapso)</name>
  <files>
src/app/api/conversations/route.ts
src/app/api/messages/[conversationId]/route.ts
  </files>
  <action>
Substituir completamente as implementacoes Kapso por stubs que:

- Retornem HTTP 200 com JSON consistente (sem exceptions) para o frontend atual.
- Incluam um campo explicito (ex: `mode: "send-only"`) para a UI saber que nao ha historico.
- `GET /api/conversations`: retornar `{ mode, data: [], paging: null }`.
- `GET /api/messages/:conversationId`: retornar `{ mode, data: [], paging: null }`.
- Remover imports e tipos de `@kapso/whatsapp-cloud-api` (buildKapsoFields, MetaMessage, etc).
  </action>
  <verify>
`rg -n \"@kapso/whatsapp-cloud-api\" src/app/api/conversations src/app/api/messages\\[/conversationId\\] -S`
  </verify>
  <done>
As duas rotas compilam sem Kapso e a UI deixa de logar erros de fetch repetidamente.
  </done>
</task>

<task type="auto">
  <name>Task 2: ConversationList - permitir iniciar chat por numero (start new chat)</name>
  <files>
src/components/conversation-list.tsx
  </files>
  <action>
Evoluir a UX para suportar modo send-only:

- Quando `filteredConversations.length === 0` e `searchQuery` parece um telefone (minimo: so digitos e `+`), mostrar acao "Start chat with {searchQuery}".
- Ao clicar, chamar `onSelectConversation` com um objeto conversation local:
  - `id`: usar o proprio `phoneNumber` (ou `temp:{phoneNumber}`)
  - `phoneNumber`: `searchQuery`
  - `status`: "active"
  - `lastActiveAt`: `new Date().toISOString()`
- Opcional (mas recomendado): manter essas conversas locais em state para aparecerem na lista mesmo se o backend retornar vazio.
- Mostrar um aviso discreto quando o backend estiver em `mode: "send-only"` ("Historico sera habilitado na Fase 1").
  </action>
  <verify>
Rodar `npm run build` e abrir a UI: e possivel iniciar chat digitando um numero no campo de busca.
  </verify>
  <done>
Sem dados do backend, o usuario ainda consegue abrir um chat e chegar na tela de envio.
  </done>
</task>

<task type="auto">
  <name>Task 3: MessageView - estado local (optimistic) e remover tipos Kapso</name>
  <files>
src/components/message-view.tsx
  </files>
  <action>
Melhorar `MessageView` para funcionar sem historico:

- Remover `import type { MediaData } from '@kapso/whatsapp-cloud-api'` e substituir por um type local minimo (somente campos usados no componente).
- Quando o backend responder `mode: "send-only"`, evitar sobrescrever mensagens locais com lista vazia.
- Ao enviar mensagem com sucesso (`/api/messages/send`), inserir uma mensagem outbound no state local (optimistic) com:
  - `id`: usar id retornado (se houver) ou um id temporario
  - `direction`: "outbound"
  - `content`: texto/caption
  - `createdAt`: now
  - `status`: "sent" (placeholder)
- Adicionar flag de dev para liberar envio de mensagens regulares mesmo sem inbound:
  - env client-side: `NEXT_PUBLIC_INBOX_DEV_ALLOW_FREEFORM=true`
  - quando ativa, `canSendRegularMessage` fica `true` e a UI mostra um aviso pequeno ("Dev override").
  </action>
  <verify>
Rodar `npm run build` e testar: iniciar chat por numero, enviar mensagem, e ver a mensagem aparecer na tela sem depender de `/api/messages/:id`.
  </verify>
  <done>
UI funciona em modo send-only, sem dependencias do Kapso e sem bloquear totalmente o envio em desenvolvimento.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passa.
- UI permite iniciar chat e enviar mensagem (com `NEXT_PUBLIC_INBOX_DEV_ALLOW_FREEFORM=true` quando necessario).
- `rg -n \"@kapso/whatsapp-cloud-api\" src/components -S` nao retorna resultados.
</verification>

<success_criteria>
- Inbox endpoints nao usam Kapso e retornam um modo claro (send-only).
- Frontend continua utilizavel para validar integracao Meta ate a Fase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/00-remover-kapso/00-03-SUMMARY.md`.
</output>

