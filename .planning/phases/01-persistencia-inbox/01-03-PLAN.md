---
phase: 01-persistencia-inbox
plan: 3
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/app/api/conversations/route.ts
  - src/app/api/messages/[conversationId]/route.ts
autonomous: true
must_haves:
  truths:
    - "A UI lista conversas a partir do DB (ordenadas por ultima atividade)."
    - "A UI lista mensagens de uma conversa a partir do DB (ordenadas por timestamp)."
    - "Os formatos retornados continuam compativeis com o frontend atual."
  artifacts:
    - "Rotas `/api/conversations` e `/api/messages/:conversationId` consultando Prisma"
  key_links:
    - "conversationId usado no frontend deve ser um ID interno (DB) consistente"
    - "Queries sempre filtram por tenantId (default ate Fase 2)"
---

<objective>
Trocar as rotas stub (send-only) por implementacao real baseada no banco, para habilitar inbox com historico.

Purpose: UI passa a refletir mensagens recebidas/enviadas via webhooks persistidas.
Output: endpoints de leitura do inbox ligados ao DB.
</objective>

<execution_context>
@.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/phases/01-persistencia-inbox/01-CONTEXT.md
@.planning/phases/01-persistencia-inbox/01-RESEARCH.md
@src/app/api/conversations/route.ts
@src/app/api/messages/[conversationId]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar GET /api/conversations lendo do DB</name>
  <files>
src/app/api/conversations/route.ts
  </files>
  <action>
Implementar `GET /api/conversations` usando Prisma:

- Resolver `tenantId` via `getOrCreateDefaultTenant()`.
- Suportar `limit` via querystring (max 100).
- Retornar `{ mode: "db", data, paging: null }` (manter `mode` para compatibilidade com Fase 0).
- Cada item deve casar com o shape esperado pelo frontend:
  - `id` (conversationId do DB)
  - `phoneNumber` (wa_id ou E164 do contato, conforme modelagem)
  - `status`
  - `lastActiveAt`
  - `phoneNumberId` (nosso phone_number_id)
  - `contactName` (quando disponivel)
  - `messagesCount`
  - `lastMessage` { content, direction, type }

Importante:

- Ordenar por `lastActiveAt desc`.
- Garantir filtragem por tenantId sempre.
  </action>
  <verify>
`npm run build` e `curl /api/conversations` retorna lista quando DB tem dados.
  </verify>
  <done>
Lista de conversas na UI passa a refletir dados do DB.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar GET /api/messages/:conversationId lendo do DB</name>
  <files>
src/app/api/messages/[conversationId]/route.ts
  </files>
  <action>
Implementar `GET /api/messages/:conversationId` usando Prisma:

- Resolver `tenantId` via `getOrCreateDefaultTenant()`.
- Validar que a conversa pertence ao tenant.
- Suportar `limit` via querystring.
- Retornar `{ mode: "db", data, paging: null }`.
- Mapear cada message para o shape do frontend:
  - `id` (meta message id ou id interno, mas consistente)
  - `direction` inbound/outbound
  - `content`
  - `createdAt` ISO
  - `status` (ultima status conhecida para outbound)
  - `phoneNumber`
  - `hasMedia`, `mediaData` quando aplicavel (usar `metadata.mediaId` para `/api/media/:id`)
  - `messageType`, `caption`, `filename`, `mimeType`

Regras:

- Ordenar por timestamp asc (como a UI espera).
- Evitar N+1: buscar statuses de forma eficiente (join/select).
  </action>
  <verify>
`npm run build` e UI carrega mensagens sem erro para uma conversa com dados.
  </verify>
  <done>
Historico aparece no MessageView sem depender de estado local do modo send-only.
  </done>
</task>

</tasks>

<verification>
- Com webhooks alimentando DB, UI exibe conversas e mensagens.
- Rotas nao tem referencia Kapso.
</verification>

<success_criteria>
- Inbox real (read-side) funcionando via DB.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistencia-inbox/01-03-SUMMARY.md`.
</output>

