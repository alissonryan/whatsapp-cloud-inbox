---
phase: 01-persistencia-inbox
plan: 4
type: execute
wave: 3
depends_on: [2, 3]
files_modified:
  - src/app/api/messages/send/route.ts
  - src/app/api/messages/interactive/route.ts
  - src/app/api/templates/send/route.ts
  - src/lib/webhooks/meta-whatsapp.ts
autonomous: true
must_haves:
  truths:
    - "Mensagens outbound enviadas pela UI aparecem no historico (DB) imediatamente."
    - "Status (sent/delivered/read/failed/played) atualiza mensagens outbound quando webhook chega."
    - "Regra de 24h e aplicada no server (nao so no UI)."
  artifacts:
    - "Rotas de envio escrevem no DB (Message + Conversation updates)"
    - "Ingestao de webhook atualiza status no DB"
  key_links:
    - "Send response (wamid) -> persist metaMessageId -> status webhook consegue encontrar e atualizar"
    - "Checagem de 24h baseada no ultimo inbound (por conversa) para bloquear freeform quando necessario"
---

<objective>
Conectar o write-side (envio) ao DB e aplicar a regra de 24h no server, para que o inbox fique consistente e seguro.

Purpose: historico e status ficam confiaveis e independentes de estado local do client.
Output: envio + persistencia + status update (via webhook) funcionando junto.
</objective>

<execution_context>
@.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/phases/01-persistencia-inbox/01-CONTEXT.md
@src/app/api/messages/send/route.ts
@src/app/api/messages/interactive/route.ts
@src/app/api/templates/send/route.ts
@src/lib/webhooks/meta-whatsapp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistir outbound (text/media/interactive) e aplicar regra de 24h no server</name>
  <files>
src/app/api/messages/send/route.ts
src/app/api/messages/interactive/route.ts
  </files>
  <action>
Atualizar rotas de envio para:

- Resolver `tenantId` (default) e obter/garantir `Conversation` para o contato (wa_id).
- Antes de enviar uma mensagem freeform (text/media/interactive), verificar janela de 24h:
  - Se nao houver inbound registrado ou ultimo inbound > 24h, bloquear com erro claro (ex: 422) sugerindo template.
  - Permitir templates sempre (tratado no Task 2).
- Enviar via Meta (Cloud API).
- Persistir `Message` outbound no DB com:
  - `metaMessageId` (wamid da resposta)
  - `direction=outbound`, `type`, `content/caption`, `timestamp=now`
  - `mediaId` quando aplicavel
  - `status="sent"` (inicial)
- Atualizar `Conversation.lastActiveAt` e contadores/previews.
  </action>
  <verify>
Enviar uma mensagem pela UI e validar no DB: Message criada com metaMessageId e aparece no GET `/api/messages/:conversationId`.
  </verify>
  <done>
Envio cria historico imediatamente e regra 24h e aplicada no server.
  </done>
</task>

<task type="auto">
  <name>Task 2: Persistir outbound de templates (sempre permitido)</name>
  <files>
src/app/api/templates/send/route.ts
  </files>
  <action>
Atualizar envio de template para tambem persistir no DB:

- Mesma resolucao de `tenantId` + `Conversation`.
- Enviar template via Meta e capturar `metaMessageId`.
- Persistir `Message` outbound com `type="template"` e campos relevantes (nome do template, parametros como JSON se necessario).
- Atualizar `Conversation.lastActiveAt` e preview.
  </action>
  <verify>
Enviar template pela UI e confirmar:
1) registro no DB
2) conversa atualiza preview/lastActiveAt
  </verify>
  <done>
Templates enviados aparecem no historico e mantem o inbox consistente.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar ingestao para status updates consistentes</name>
  <files>
src/lib/webhooks/meta-whatsapp.ts
  </files>
  <action>
Garantir que quando chegar `statuses[]`:

- O status seja associado a `Message` outbound via `metaMessageId`.
- Atualize o "status atual" da mensagem (ex: campo `Message.lastStatus`) e/ou insira em `MessageStatus` (historico).
- Atualize timestamps de conversa se fizer sentido (ex: delivered/read nao altera lastActiveAt; inbound sim).
- Continue idempotente.
  </action>
  <verify>
Simular/reproduzir um status webhook (sent/delivered/read) e ver refletir na UI (ticks) via endpoints DB.
  </verify>
  <done>
UI mostra status correto para outbound e o DB mantem historico sem duplicacao.
  </done>
</task>

</tasks>

<verification>
- Enviar outbound -> aparece no DB sem depender de optimistic local.
- Webhook status -> atualiza status exibido no UI.
- Freeform bloqueado fora da janela de 24h (server-side).
</verification>

<success_criteria>
- Inbox fica consistente: read-side e write-side alinhados no DB.
- Regra de 24h aplicada corretamente.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistencia-inbox/01-04-SUMMARY.md`.
</output>

