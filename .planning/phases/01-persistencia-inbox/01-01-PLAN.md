---
phase: 01-persistencia-inbox
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prisma/schema.prisma
  - prisma/migrations/*
  - src/lib/db.ts
  - src/lib/tenant.ts
autonomous: false
user_setup:
  - service: postgres
    why: "Persistir conversas/mensagens/status para inbox real"
    env_vars:
      - name: DATABASE_URL
        source: "Seu provedor (Supabase/Neon/RDS/local)"
must_haves:
  truths:
    - "Existe um banco configurado e acessivel pela app (Prisma) para persistir inbox."
    - "O schema suporta multi-tenant (tenant_id em tudo), mesmo que o modo inicial seja single-tenant."
  artifacts:
    - "prisma/schema.prisma com modelos base (Tenant, Contact, Conversation, Message, MessageStatus)"
    - "src/lib/db.ts exportando PrismaClient singleton"
    - "src/lib/tenant.ts com bootstrap de tenant default (ate existir auth)"
  key_links:
    - "Webhook e APIs internas conseguem obter tenant_id (default) sem auth (temporario)"
    - "IDs/constraints garantem idempotencia (message id unico, etc.)"
---

<objective>
Introduzir persistencia (Postgres + Prisma) e um schema minimo para suportar inbox via webhooks.

Purpose: habilitar a Fase 1 (webhooks + leitura do DB) com base solida e preparada para multi-tenant.
Output: Prisma configurado, schema inicial e helpers (`db`, `tenant`).
</objective>

<execution_context>
@.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-persistencia-inbox/01-CONTEXT.md
@.planning/phases/01-persistencia-inbox/01-RESEARCH.md

# Codebase map:
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Provisionar Postgres e setar DATABASE_URL</name>
  <action>
Provisionar um Postgres (local ou cloud) e preencher `DATABASE_URL` no ambiente de desenvolvimento.
  </action>
  <steps>
1. Escolher provedor (local, Supabase, Neon, etc.)
2. Criar DB e usuario
3. Setar `DATABASE_URL` (com SSL se necessario)
  </steps>
  <resume-signal>
DATABASE_URL configurada e acessivel a partir do `npm run dev`.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Adicionar Prisma + schema base (multi-tenant ready)</name>
  <files>
package.json
package-lock.json
prisma/schema.prisma
prisma/migrations/*
src/lib/db.ts
  </files>
  <action>
- Adicionar Prisma ao projeto (`prisma`, `@prisma/client`) e configurar scripts.
- Criar `prisma/schema.prisma` com:
  - `Tenant`
  - `WhatsappPhoneNumber` (ou equivalente) para mapear `phone_number_id` por tenant
  - `Contact` (wa_id, nome opcional)
  - `Conversation` (tenant + phone_number + contact, lastActiveAt)
  - `Message` (meta_message_id unico, direction, type, content/caption, timestamp, mediaId opcional)
  - `MessageStatus` (message + status + timestamp + payload opcional)
- Criar `src/lib/db.ts` exportando `prisma` singleton (evitar leak em hot-reload).
- Criar indices/unique constraints para idempotencia:
  - `Message.metaMessageId` unique
  - `Contact.waId` unique (por tenant)
  - `Conversation` unique (tenant + phone_number_id + wa_id)
  </action>
  <verify>
`npx prisma validate` e `npx prisma migrate dev` (ou `db push`) e `npm run build`
  </verify>
  <done>
Schema aplicado no Postgres e Prisma Client gerado, com constraints que suportam dedup.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap de tenant default (temporario, ate existir auth)</name>
  <files>
src/lib/tenant.ts
  </files>
  <action>
Criar um helper `getOrCreateDefaultTenant()` que:

- Procure um tenant com slug fixo (ex: `default`)
- Se nao existir, crie com `name: "Default"`
- Retorne `tenantId` para ser usado por webhooks e APIs internas enquanto nao existe auth/multi-tenant completo.

Regras:

- Nao deve rodar migracoes; apenas CRUD normal via Prisma.
- Manter isolado para remover/alterar facilmente na Fase 2.
  </action>
  <verify>
`npm run build`
  </verify>
  <done>
Temos um caminho deterministico para obter `tenantId` sem auth (temporario).
  </done>
</task>

</tasks>

<verification>
- Prisma valida/migra sem erros.
- Build passa.
</verification>

<success_criteria>
- DB e Prisma prontos para receber webhooks.
- Schema preparado para multi-tenant (tenant_id em tudo).
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistencia-inbox/01-01-SUMMARY.md`.
</output>

